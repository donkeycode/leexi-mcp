---
description: Fetch a specific Leexi call by UUID and display it with full transcript.
argument-hint: "<uuid>"
---

Fetch a Leexi call by UUID and display it. Use `leexi_get_call({ uuid: "<arg>", include_transcript: true })`. Then show: title, date, duration, participants, summary, then the full transcript with speaker labels.

$ARGUMENTS
