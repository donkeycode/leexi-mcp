---
description: List today's Leexi calls (limit 20). Add "unprocessed" to filter unprocessed only.
argument-hint: "[unprocessed]"
---

List today's Leexi calls. Use the `leexi_list_calls` tool with `since` set to today 00:00 UTC and `limit: 20`. If the argument is "unprocessed", also set `only_unprocessed: true`. Summarize the results in a compact table: time · title · participants · duration.

$ARGUMENTS
